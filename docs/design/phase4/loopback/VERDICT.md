# Phase 3 Loop-back — Verdict

## PRISTINE: NO

NOT PRISTINE. The loop-back is incomplete on two independent axes, and the re-stress confirms 2 of 8 scenarios (config-binding-path, diagnostics-tiers) still carry blocker-severity breaks with blockersCleared=false.

AXIS 1 — a whole cluster was never applied. The 6 'in-force' fixes are C1/C3/C4/C5/C6/C7. Cluster C2 (the convergence team's @Value-Tier-2 + config-bind-locus + anti-DCE-edge-vs-R0 + registry validate-placement + env-06 lenient + BootstrapSettings 8th-field reconciliation) is NOT in that set. Its anti-DCE half rode in via the C1-authored 13-doc (so autoconfig-backoff and discovery-antidce converge), but the @Value dry-run, the config-bind locus, and the validate-placement halves are uncarried. These are exactly the residual blockers of the two failing scenarios. C2 must be applied as a fresh loop-back.

AXIS 2 — the 6 'applied' fixes were written only into the fix payload and (partially) into 13-container-lifecycle.md; they were NOT propagated into their OWNING Phase-3 docs. On-disk verification: SEAMS.md C6 'RoleTier plus order table' is still the literal placeholder stub ('Define RoleTier and a cmp_chain / Affects: a,b / Rationale: x;y'); 03-injection has 0 mentions of any C3/C4 type; 08-proxy has 0 mentions of any C3/C6 type and still names after_init as the sole install seam; 01-registry has 0 RoleTier/cmp_chain AND its line-32 refresh-template still says validate runs INSIDE refresh (the C1 validate-placement contradiction); 12-events still claims it 'REUSES leaf_core::aop::Interceptor verbatim ... no new around-advice trait' (the exact C5 type-false claim); 14 BootstrapSettings is still 7-field with no startup_validation; 06 still says '@Value reads stay lenient ... for parity'; and the 13-doc itself ships the C1 single-budget drain(grace) sketch (line 134-137) plus an internal R5-vs-ripple contradiction (line 38 eager-bitset includes singleton config beans vs line 168 'eager-excluded-because-prebound') that the C1 authoring INTRODUCED.

So a reader building from the authoritative docs would mis-build (unconditional after_init -> the double-proxy C3 was meant to kill; verbatim Interceptor reuse -> the type-impossible C5 path; validate-inside-refresh -> wrong RunState-at-failure). Composition holds in the trace ONLY because the fixes are asserted in-force; the docs do not agree.

Net: 5 residual blockers (3 from un-applied C2, 1 validate-placement, 1 fix-introduced 13-R5 self-contradiction) and 4 new loop-backs (apply C2; reconcile registry-01; propagate C1/C3/C4/C5/C6/C7 into owning docs incl. authoring the C6 seam and the two-budget drain; pin facility-quiesce pre-LIFO in shutdown step 5). Another iteration is required. Per-crate: the 3 previously-blocked crates do NOT unblock — leaf-core stays blocked (its fix-mandated ABI is unlanded), leaf-boot stays blocked (validate-placement + C2 + R5 contradiction), and leaf-tx moves to ready-after-reconciliation (its C7 mechanism is sound, only the 09-doc TxFinalizeEntry pin is unwritten). The remaining ~12 are ready-after-reconciliation (doc drift / C2-dependent) and 7 are cleanly ready.

## Residual blockers
- config-binding-path / diagnostics-tiers blocker #1 — @Value coercion locus (binding-07 lines 53/55 + injection-03 line 21 + seam-5 ripple line 118): a malformed @Value coercion (e.g. order.max-retries=abc -> u16) still surfaces only at Tier-3 runtime (inside Provider::provide at R5), NOT in the one aggregated Tier-2 AssemblyReport. The prescribed C2 fix (a throwaway validate-time @Value dry-run sub-pass calling resolve_into::<T>/from_config_value and discarding the value) is NOT among the 6 applied fixes (C1/C3/C4/C5/C6/C7) and was never written into any doc. So the diagnostics-tiers central promise 'F1+F2+F3 surface together in ONE report' still fails.
- config-binding-path blocker #2 / diagnostics-tiers blocker #2 — config-properties bind+validate LOCUS contradiction unresolved across three docs: binding-07 (lines 11/43) says @ConfigurationProperties binding runs at App<Resolve>->App<Wired> aggregating Tier-2, but those types ARE ordinary beans whose Provider::provide runs at refresh R5 per the authored 13-doc; injection-03 line 154 still says validate() runs resolve_one with NO carve-out for the one bean class validate() must materialize. For DB_POOL_SIZE=0, range(min=1) lands at an undetermined tier (validate() Tier-2 vs R5 cancel-cascade Failed). The cross-subsystem ownership of 'materialize-config-at-validate' is reproduced, not closed.
- registry-core-01 validate-PLACEMENT contradiction (line 32 refresh-template prose + line 135 refresh() signature comment): 01-registry-core.md ON DISK still states refresh() runs 'init context services -> anti-DCE self-check -> App<Wired>::validate() -> eager-instantiate' (validate INSIDE refresh, AFTER context-services install), directly contradicting the C1-authored 13-container-lifecycle.md, bootstrap-diagnostics-14 (seal->validate->refresh), and seam #1. The seam's own C1 ripple says 'registry-core line 32 defers to THIS template' but the file was never edited. The two readings give different RunState-at-failure and different teardown obligations.
- 13-container-lifecycle.md R5 self-contradiction INTRODUCED by the C1 authoring (line 38 eager bitset vs line 168 ripple note): R5's normative text defines the eager bitset as excluding only 'lazy/scoped/prototype' and force-including smart-init + Role::Infrastructure — a singleton Application config bean (DbProps) matches NO exclusion, so per the literal R5 spec it is eager-instantiated via Engine::create->Provider::provide (re-binding), routing a bind failure into the R5 cancel-cascade (RunState->Failed) rather than the promised Tier-2 aggregation. The ripple note (line 168) asserts the opposite ('eager-excluded-because-prebound'). A fix-introduced internal blocker.
- Unified strictness lever vs config-properties JSR validation under StartupValidation::Skip (seam-5 + binding-07): the sibling blocker — Skip silently eliding the range/nested JSR checks that have no Tier-3 fallback — survives only as a doc-13 remaining-RISK bullet, not pinned into the lever's definition; it is parasitic on the unclosed bind-locus blocker above, so it cannot be considered closed either. Compounded by 14-bootstrap-diagnostics BootstrapSettings still being the 7-field type with NO startup_validation field on disk — the lever's mandated home is unbacked.

## New loop-backs needed
- to phase3: Land cluster C2 (the un-applied 6th reconciliation, scoped out of the applied C1/C3/C4/C5/C6/C7 set): (a) pin a validate-time throwaway @Value dry-run sub-pass (resolve_into::<T>/from_config_value, discard value, collect Convert at Tier-2) in injection-03 + binding-07; (b) reconcile the config-properties bind LOCUS to ONE site (config beans materialized at App<Wired>::validate(), R5 eager bitset edited to exclude pre-bound config beans) across binding-07 / injection-03 / 13-container-lifecycle R5 so they stop contradicting; (c) delete env-06's 'lenient @Value reads for parity' line and replace with the strict-collect-into-AssemblyReport wording; (d) add the 8th field BootstrapSettings.startup_validation: StartupValidation to 14-bootstrap-diagnostics; (e) fold the Skip carve-out for config-properties JSR validation into the strictness lever definition. Without C2, config-binding-path and diagnostics-tiers do not reach pristine.
- to phase3: Reconcile registry-core-01 line 32 (and the line-135 refresh() comment) to validate-BEFORE-refresh per the authored 13-container-lifecycle template + seam #1 — the seam's own ripple ('line 32 defers to THIS template') was never applied to disk, leaving a load-bearing cross-doc contradiction on validate placement / RunState-at-failure / teardown obligation.
- to phase3: Propagate the 6 'in-force' fixes (C1/C3/C4/C5/C6/C7) into their OWNING Phase-3 docs, which are all still pre-fix on disk: author the SEAMS.md C6 'RoleTier plus order table' section (still the literal stub 'Define RoleTier and a cmp_chain / Affects: a,b / Rationale: x;y'); add C3/C4 types to 03-injection (scope_and_advice_check, AdvisedConcreteInjection, lifetime_rank/outlives, install_scoped_proxy, ScopeNotActive, ScopeMismatchClass — currently 0 mentions); add C3/C6 to 08-proxy (proxy_install_site/ProxyInstallSite, OwnedTarget, cmp_chain/ChainKey, scoped_proxy_cache, two-site rule — currently 0 mentions; 08 still names after_init as the SOLE install seam); add RoleTier/cmp_chain to 01-registry; rewrite 12-events from 'REUSES leaf_core::aop::Interceptor verbatim / no new around-advice trait' (lines 124/159/166) to the C5 DispatchInterceptor + ListenerNext + publish-time-condition design; write C7's TxFinalizeEntry-on-per-request-ledger into 09-declarative-advice; reconcile 13-container-lifecycle's Public API sketch (lines 134-137) and context-close step 2 (line 59) from the C1 single-grace drain(grace) / DrainOutcome{Quiesced,CancelledAfterGrace{drained,ledgers_run}} to the C7 two-budget drain(grace,finalize_budget) / +finalize_overruns + BootstrapSettings.shutdown.finalize_grace + the registry-holds-store-and-drains (not SpawnHandle-abort) wording. Relocate proxy_install_site placement explicitly into leaf-core (it is called by Engine::create AND the resolver, both leaf-core; 'owned by container-lifecycle' is conceptual only).
- to phase3: Pin the facility/multicaster-quiesce as an explicit PRE-LIFO step in shutdown step 5 (or include detached tx-deferred @Async SpawnHandles in the step-2 outstanding count): under the authored 13-doc step-5 pure LIFO=reverse-publication, the Infrastructure multicaster (early-published) drains LAST while an Application @Async-listener host (late-published) drains FIRST, so a straggler deferred @Async listener can touch a logically-close()'d host in the shutdown window — the exact inversion the C5/C7 'host destruction after facility drain' claim forbids. Memory-safe via Arc, but a logical-teardown ordering hazard the fix overstates as closed.

## Per-crate readiness

- **leaf-core** — `blocked` — Still blocked. The ultra-stable ABI it must mint for the in-force fixes is not landed on disk: RoleTier enum + cmp_chain/ChainKey (SEAMS C6 still a stub, 01/08 have 0 mentions), the C5 DispatchInterceptor/ListenerNext sibling trait (12-events still claims verbatim aop::Interceptor reuse), the C4 ScopeMismatchClass/lifetime_rank/AdvisedConcreteInjection (03 has 0 mentions), proxy_install_site/OwnedTarget (08 has 0 mentions), and the C7 two-budget RequestScopeRegistry::drain/DrainOutcome shape (13 sketch still single-budget). C2 also adds the StartupValidation enum + BootstrapSettings 8th field to this crate's ABI, unlanded. Pre-1.0 ABI cannot freeze until these land.
- **leaf-boot** — `blocked` — Still blocked. Owns the validate-placement contradiction: registry-core-01 line 32 (its refresh template) still says validate runs INSIDE refresh after context-services, contradicting the authored 13-doc / seam-1. Owns the C2 config-bind-locus + @Value dry-run + R5 eager-bitset-vs-ripple self-contradiction (13 line 38 vs 168), the strictness-lever validate() head, and the step-5 facility-quiesce-vs-LIFO ordering. The 13-doc was authored (C1) but still carries the single-budget drain sketch and the un-reconciled R5/ripple contradiction.
- **leaf-tx** — `ready-after-reconciliation` — Mechanism is sound under the in-force C7 fixes (TxFinalizeEntry rides the per-request InstanceStore ledger via the existing InstanceStore::ledger() seam; in-flight drain runs the awaited rollback; finalize-once OnceCell). Was 'blocked' on C7+C5; the C7 mechanism is now coherent in 13-doc, but the TxFinalizeEntry pin is not yet written into 09-declarative-advice (line 19/26 still says 'an awaited step in the interceptor OR a TeardownLedger entry' without naming WHICH ledger). Doc-reconciliation only; no remaining blocker for leaf-tx itself.
- **leaf-macros** — `ready-after-reconciliation` — Was ready-after-reconciliation on C2; C2 is still un-applied. Needs the @Value grammar-split / dry-run-emission alignment once C2's validate-time @Value dry-run is pinned. No leaf-macros-specific blocker beyond C2 propagation.
- **leaf-codegen** — `ready-after-reconciliation` — Was ready-after-reconciliation on C2; unchanged. Anchor-survival contract (provides_descriptors! emitted UNCONDITIONALLY, cfg-independent of beans) should be pinned in discovery-codegen so the R0 classify() predicate order (anchor_absent before CompiledOutByCfg) is provably sound — flagged friction from discovery-antidce, not a blocker.
- **leaf-config** — `ready-after-reconciliation` — Was ready-after-reconciliation on C2; C2 is the un-applied cluster that owns the config-bind locus, the collection-replacement Vec-under-non-enumerable tri-state, and the env-var Origin threading. These are the residual config-binding-path blockers/frictions; leaf-config cannot be blessed until C2 lands.
- **leaf-conditions** — `ready-after-reconciliation` — Was ready-after-reconciliation on C2; unchanged. The four-class R0 classify() (the anti-DCE half of C2) rode in via the C1-authored 13-doc, so the discovery-antidce / autoconfig-backoff candidate arithmetic is sound; only doc-05 stale prose (CompiledOutByCfg granularity for crate-local feature gates, ConditionReport finalize fusion) remains as friction.
- **leaf-tokio** — `ready-after-reconciliation` — Was ready-after-reconciliation on C7/C2. The C7 in-flight-drain + RequestScopeRegistry + ShutdownTrigger interaction is coherent in 13-doc; residual is the grace/finalize_budget default tuning (needs a web-load stress fixture) and the single- vs two-budget drain signature reconciliation. No blocker for the runtime crate itself.
- **leaf-smol** — `ready-after-reconciliation` — Mirror of leaf-tokio; same C7 drain-signature reconciliation. No independent blocker.
- **leaf-starter-web** — `ready-after-reconciliation` — Drives RequestScopeRegistry::enter() at the Holder::scope request boundary (C7 seam). Sound once the drain protocol prose (step-2 'registry-holds-store-and-drains' vs the authored 'SpawnHandle abort' wording, line 59) is reconciled. No blocker.
- **leaf-redis** — `ready-after-reconciliation` — Representative integration crate; auto-config Fallback rows compose under seam-3 FallbackDemote + the R0 classify(). Residual: doc-03 primary-fallback stale two-pass prose (superseded by seam-3 but un-reconciled). Friction only.
- **leaf** — `ready-after-reconciliation` — Umbrella/BOM. Depends on the leaf-core ABI freezing (blocked) and C2 landing; its own force-link + ExpectedManifest seam is unchanged. Ready once the ABI and C2 reconciliations land.
- **leaf-cache** — `ready-after-reconciliation` — Was ready-after-reconciliation on C6. cmp_chain/RoleTier and the CACHE_ORDER<TX_ORDER pin are in force in the fix payload but the C6 seam is still a SEAMS stub and 09-declarative-advice line 15/154 still cite cmp_order-only / the old chain list. Also owns the @Cacheable-on-scoped-bean cross-scope cache-sharing documented divergence + lint. Doc-reconciliation only; no blocker.
- **leaf-validation** — `ready-after-reconciliation` — Was ready-after-reconciliation on C6. The JSR range/nested validation after each Object node is sound; but its Skip-never-elides-config-validation guarantee is parasitic on the unclosed C2 bind-locus, and the env-var Origin threading (field-path<->OriginId) is an un-pinned C2 artifact. Blocked-adjacent via C2 but not itself a blocker once C2 lands.
- **leaf-resilience** — `ready-after-reconciliation` — Was ready-after-reconciliation on C6. Its RetryInterceptor/ConcurrencyLimit advisors slot into the RoleTier table (CONCURRENCY/RETRY orders); needs the C6 seam authored (still a stub) and the ListenerNext-is-forward-only note (resilience-on-events would need a different mechanism). Doc-reconciliation only.
- **leaf-aop-expr** — `ready` — Was ready; unchanged. Compile-time pointcut lowering; the dropped-runtime-interpreter audit (closure-only backend) holds. No fix touches it.
- **leaf-cron** — `ready` — Was ready; unchanged. Cron DST remains a pre-existing unresolved note, not a fix-introduced break.
- **leaf-i18n** — `ready` — Was ready; unchanged. LocaleKey holder (Propagation::Inherit) is orthogonal to all 6 clusters + C2.
- **leaf-serde** — `ready` — Was ready; unchanged. Opt-in serde-bridge converter behind a cargo feature; FromConfigValue stays canonical. Untouched by the fixes.
- **leaf-figlet** — `ready` — Was ready; unchanged. Banner default; the one deliberate fail-fast exception (Severity::Warn degrade). Untouched.
- **leaf-starter-redis** — `ready` — Was ready; unchanged. Capability starter (Cargo.toml aggregator only); no code, no ABI exposure to any cluster.
