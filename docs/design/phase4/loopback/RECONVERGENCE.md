# Phase 3 Loop-back — Re-convergence (8 scenarios re-stressed)

## `advised-scoped-concrete: a @Transactional @Cacheable request-scoped (PerContextK`
- **blockers cleared:** True
- **verdict:** PASS — the patched design COMPOSES for this scenario and BOTH prior blockers are GONE. V1 (Ref<OrderSvc>, concrete): App<Wired>::validate() now takes ProxyPlan as an explicit input and scope_and_advice_check check (a) fires ResolveError::AdvisedConcreteInjection before any constructor runs (B2/C3 cleared). V2 (Ref<dyn Order>, running): the two-site install rule (proxy_install_site=Resolver for PerContextKey) makes R4/after_init skip the scoped bean, the InstanceStore stores the bare ErasedBean, and install_scoped_proxy builds ONE OrderProxy{chain, ScopeTarget} whose AdviceChain is built once via cmp_chain over chain_key() and memoized — so tx+cache fire EXACTLY ONCE per call (B1/C3 cleared; no double-proxy, no raw handoff). The three prior frictions are addressed: no-active-scope → R2 NoScopeOpener doctor WARN + runtime ResolveError::ScopeNotActive; the scoped-proxy second installer is named (install_scoped_proxy at the resolver); @Cacheable cross-scope is a documented divergence + leaf-doctor lint; cache-outside-tx ordering holds (C2 table CACHE_ORDER=400 < TX_ORDER=500), so a hit short-circuits before tx and before scoped construction (the cosmetic note stands, correct-by-design). I found NO new SOUNDNESS break — every new finding is doc-reconciliation FRICTION: (1) the C6 \"RoleTier plus order table\" seam in SEAMS.md is still a literal stub; (2) the owning Phase-3 docs (03/08/01) and 09/12 were never updated with their C3/C4/C6/C5/C7 types — 08 still describes unconditional after_init as the sole install seam, which is the exact pre-fix model; (3) the C7 two-budget drain + TxFinalizeEntry refinement is not written into the authored 13 API sketch (still the C1 single-grace shape); (4) proxy_install_site is declared \"owned by container-lifecycle\" (leaf-boot) but must physically live in leaf-core since Engine::create and the resolver call it and deps point inward. All four are fixable-in-place by propagating the FIXES (which are coherent and in force) into the owning docs; none blocks this scenario. blockersCleared=true.
- **remaining:** [friction] The C6 pinned seam is still a literal STUB ("Define RoleTier and a cmp_chain" / "Affects: a, b" / "Rationale: x; y"). The actual C6 content (RoleTier enum, cmp_chain, ChainKey, AdvisorRef::chain_key(), the order:i32 advice+multicaster tables, the property test asserting CACHE_ORDER<TX_ORDER etc.) lives ONLY in the loop-back FIXES payload and is cross-referenced from 13-container-lifecycle (lines 19,157-158) and 08/09/12 prose. This content is load-bearing for THIS scenario (the cache-outside-tx chain order and the cmp_chain sort at both install sites), but a reader building from the authoritative seam doc alone cannot construct it. Composition still holds because the FIXES are in force; the gap is doc-reconciliation, not mechanism.; [friction] The C3/C4/C6 type+API additions are ABSENT from the docs that own them: 03 has 0 mentions of lifetime_rank/outlives/ScopeMismatchClass/AdvisedConcreteInjection/scope_and_advice_check/install_scoped_proxy/ScopeNotActive; 08 has 0 mentions of proxy_install_site/ProxyInstallSite/OwnedTarget/cmp_chain/ChainKey/chain_key/scoped_proxy_cache/two-site; 01 has 0 mentions of RoleTier/cmp_chain. 08 still describes after_init as the sole install seam (lines 14,19,28-31) and AdvisorRef{id,order,role} (line 35), and 03's PrimaryPromote/ScopeMismatch prose is the pre-widening narrow request-only version. All fixes are pinned coherently in 13-container-lifecycle.md + the FIXES payload, so the trace composes, but a reader of the owning subsystem docs alone would mis-build (e.g. unconditional after_init proxy swap → the very double-proxy B1 was supposed to kill).; [friction] Two fix-payload pins are not written into their owning docs. (a) C7's TxFinalizeEntry-rides-the-per-request-ledger pin is absent from 09 — 09 still says async rollback is "an awaited step in the interceptor OR a TeardownLedger entry" without naming WHICH ledger (the exact gap C7 closes); only 13 line 164 carries the cross-ref. (b) C5 authored a separate DispatchInterceptor trait because reusing leaf_core::aop::Interceptor verbatim is type-impossible, but 12-events still claims the multicaster REUSES leaf_core::aop::Interceptor verbatim. Neither is load-bearing for THIS scenario (no mid-flight shutdown exercises TxFinalizeEntry here; events only fire Refreshed/Closed facts), so they remain friction for this trace, but they are unreconciled relative to the in-force fixes.
- **NEW:** [friction] C3 declares `proxy_install_site(Multiplicity)->ProxyInstallSite` "owned by container-lifecycle" (= leaf-boot) yet "read identically by Engine::create AND injection-resolution::resolve()" — both of which live in leaf-core (TOPOLOGY: leaf-core owns the Engine::create driver and the whole resolve spine; leaf-boot deps point INWARD to leaf-core, never the reverse, no cycles). If the helper literally lived in leaf-boot, leaf-core could not call it (upward dep / cycle). It is a pure match on the leaf-core Multiplicity enum returning a leaf-core ProxyInstallSite, so it MUST physically reside in leaf-core; "owned by container-lifecycle" can only mean conceptual ownership. 13's crate-hints (lines 173-174) list RunState/RequestScopeRegistry/cmp_chain in leaf-core but omit proxy_install_site entirely, leaving the placement ambiguous. Trivially fixable (relocate to leaf-core alongside Multiplicity); the mechanism composes. Friction, not a blocker.; [friction] The authored 13 doc still reflects the C1 single-grace drain — enter() (no args), drain(grace: Deadline), DrainOutcome{Quiesced, CancelledAfterGrace{drained, ledgers_run}} — whereas the C7 fix now in force specifies enter(store, cancel), drain(grace, finalize_budget) (two budgets), DrainOutcome{...CancelledAfterGrace{drained, ledgers_run, finalize_overruns}}, plus BootstrapSettings.shutdown.finalize_grace and the TxFinalizeEntry. The authoritative doc contradicts the in-force fix. Dormant for THIS scenario (steady-state request + normal teardown; no SIGTERM-mid-request to exercise the two-budget split), so it is friction here, but it is a genuine doc-vs-fix divergence the patch introduced by refining C1's sketch without updating the authored API block.

## `autoconfig-backoff: starter auto-config beans gated by conditions+profile; user `
- **blockers cleared:** True
- **verdict:** COMPOSES — the autoconfig-backoff scenario is SOUND end-to-end on the patched design, and the prior blocker class for this candidate-arithmetic path ('FallbackDemote vs anti-DCE: all-fallback silently wins') is CLOSED. Trace: source/macro emit one Descriptor per autoconfig bean into AUTO_CONFIGS with candidate_role={Plain,fallback=true} (RedisAutoConfig→RedisClient gated OnProperty+OnProfile, CacheAutoConfig→CacheManager, MetricsAutoConfig→MetricsRecorder); the user's override emits a Normal RedisClient into COMPONENTS; the exclude lowers to a ContractId in ExclusionSet. App<Define> lift + the CHEAP anchor-presence self_check at the Define→Resolve edge. App<Resolve>: route_conditions evaluates OnProfile (over the sealed ActiveProfiles materialized in seal_environment) and OnProperty (over sealed Env); run_autoconfig runs the exclude>back-off>default ladder — the excluded CacheAutoConfig mints NO BeanId (ConditionReport::Exclusion, structurally absent from both candidate set and expected count, no subtraction needed); a profile/property miss is recorded as a silent Negative; survivors register_lite incrementally so each DefinitionProbe.would_resolve_unique sees the growing set. seal() freezes the dense registry + ContractId guard; ProxyPlan is EMPTY (no advisors). App<Wired>::validate() runs resolve_one per eager point: for RedisClient, primary_promote runs seam-3's FallbackDemote FIRST → the user's Normal supersedes the autoconfig Fallback → Unique(user) (override works, fallback demote dominates even @Primary); for MetricsRecorder, the lone Fallback flows on as its own working set → Unique(Fallback) (the soft default wins, not silent — it is the only candidate). Fixes #3/#4's scope_and_advice_check is inert (empty ProxyPlan → check (a) never fires; no by-value shorter-scope injection → check (b) never fires). refresh(): R0's classify() reconcile finds no anchor_absent crate (all participating crates contributed rows) so no SourceVanished; R1–R8 install + eager-wave proceed; steady-state resolution reads the frozen winners lock-free; teardown drains the container ledger LIFO (no scoped/advised beans, so the C7 in-flight-drain and per-request tx finalization paths are not exercised here and stay inert/correct). REMAINING: one friction (doc-03 primary-fallback prose still describes the OLD two-pass order + the OLD anti-DCE-at-validate precondition, both superseded by seam-3 + the C1-authored R0 classify — a stale-prose/diagnostic-locus inconsistency, harmless for this scenario since the user bean is present). NEW: no break the fixes INTRODUCED into the design; only a cosmetic observation that C2 itself is absent from the 6 applied fixes (its anti-DCE half rode in via C1, but its 'FallbackDemote-after-R0' sequencing + @Value two-sub-pass reconciliation are uncarried — a loop-back-set coverage gap, not a regression). blockersCleared=true: zero blocker-severity breaks remain for this scenario.
- **remaining:** [friction] DIAGNOSTIC-LOCUS / STALE-PROSE inconsistency, not a soundness break for this scenario. doc-03's primary-fallback text still asserts (a) the OLD two-pass order 'PASS 1 unique @Primary; PASS 2 (only if zero primaries) partition regulars/fallbacks' — which seam-3 explicitly REVERSED to FallbackDemote-FIRST so a @Fallback@Primary can never beat a plain Normal; and (b) that the anti-DCE expected-count self-check is a 'hard PRECONDITION ... surfaced as a NoSuchBean naming the missing source crate' AT the validate() pass. The C2 reconciliation (embodied in the authored 13-container-lifecycle R0 classify()) RELOCATED that reconcile to R0 at refresh, AFTER validate(). Net effect for the lone-@Fallback-wins-on-vanish edge: if a user's regular-bean crate is DCE-dropped, validate() now passes WITH the Fallback as the Unique winner (no error there), and the loud catch is deferred to R0's classify() → SourceVanished(loud) before any constructor runs. Sound (no wrong instance is ever built), but the diagnostic the doc-03 prose promises (a validate-time NoSuchBean naming the crate) is delivered instead as a refresh-time AntiDceError::SourceVanished, and doc-03's stale two-pass/precondition prose now contradicts the authoritative pins. For the literal scenario (user bean PRESENT, exclude works, Fallback genuinely the lone survivor) this never bites — the winner arithmetic is correct at validate and R0 finds nothing to vanish.
- **NEW:** [cosmetic] The 6 fixes provided as 'in force' are C1 (container-lifecycle), C6 (RoleTier/cmp_chain), C4 (ScopeMismatch), C3 (proxy two-site), C5 (events DispatchInterceptor), C7 (teardown drain). C2 ('@Value coercion Tier-2 + config-bind locus + anti-DCE edge-vs-R0 + lone @Fallback wins on vanish') is NOT in the applied set. The scenario is fortunately still sound because the C1 fix AUTHORED 13-container-lifecycle with the R0 row-count classify() (anchor_absent→SourceVanished / all-Negative→silent / all-cfg→CompiledOutByCfg / else loud) already baked in — i.e. the anti-DCE HALF of C2 rode in on C1. But the OTHER half of C2 — the 'two validate-time sub-passes' for @Value coercion and the 'FallbackDemote after R0' sequencing reconciliation — is NOT carried by any of the 6 fixes and leaves doc-03's primary-fallback prose un-reconciled (the friction above). This is not a NEW break the fixes INTRODUCED into the running design; it is a coverage gap in the loop-back set itself. It does not block this scenario.

## `cycle-deferral-prototype: A<->B constructor cycle broken with user LazyRef + a s`
- **blockers cleared:** True
- **verdict:** COMPOSES, BLOCKER CLEARED. The single original blocker (stateful prototype-into-singleton by-value) is GONE under the patched design. C4's widened terminal scope_and_advice_check fires its ShorterScopedByValue branch for Coordinator{w: Worker}: the injection point is PointKind::Bean (the macro's prototype-param owned-move inference keeps the kind as Bean, per the original trace step 1, not Deferral), holder Coordinator is Once (lifetime_rank 0), target Worker is PerResolution (lifetime_rank u8::MAX), so outlives(Once, PerResolution) = true and validate() raises a loud ResolveError::ScopeMismatch{ShorterScopedByValue} with the Lookup<Worker> fix-hint BEFORE any constructor runs, aggregated into the one AssemblyReport. Both fused defects of the original blocker are now closed at the earliest legible tier: a Sync-but-stateful Worker is caught by C4 at App<Wired>::validate(); a !Sync Worker is caught one tier earlier at compile time by the ShareableBean #[diagnostic::on_unimplemented] (concurrency-contract). Per the pinned strictness lever, ScopeMismatch is explicitly named as structural soundness that stays HARD even under Lenient, so the rejection cannot be silently downgraded. The check correctly does NOT false-positive on the legitimate sub-scenarios: A's b: LazyRef<dyn BSvc> is PointKind::Deferral (exempt + edge removed from the cycle graph, so A->B cycle still classifies clean); B's a: Ref<dyn ASvc> is Once-into-Once (outlives = false); Pipeline's Vec<Ref<dyn Step>> is Cardinality::Multiple and bypasses resolve_one (and thus the terminal check) entirely; check (a) AdvisedConcreteInjection does not fire because the empty ProxyPlan has no advisors. The three named loop-back frictions for this scenario are all now pinned: C1 pins the wave-partition invariant in R5 (every mandatory edge -> strictly-earlier wave; intra-wave = independent-only, with the OnceCell as the same-bean arbiter), C1 rewords the 'init HOLDS NO GUARD' claim to 'no separate/global/cross-bean lock; self-re-entrant init is a cycle caught at validate()', and seam-3 documents Fallback-in-collection semantics. C5/C3/C6's advice machinery is not exercised (no advised bean here); C6's cmp_chain governs only R2/R3/R6 infrastructure ordering (all tier-0 Infrastructure, deterministic) while the Pipeline collection correctly keeps plain cmp_order; C7's in-flight drain is a no-op (outstanding==0 -> Quiesced instantly) for this non-web app, so teardown proceeds cleanly to stop_all -> container TeardownLedger LIFO. I found NO new soundness break introduced by the fixes. The only residual items are: the intentional, now-documented prototype async-teardown gap (friction), the imprecise 'or LazyRef<Worker>' fix-hint text for an owned prototype (cosmetic, inherited from the original proposal), and the documented Fallback-in-collection DX divergence (cosmetic). The 13-container-lifecycle stub is fully and consistently authored from seam-1; the refresh/teardown template walks cleanly end to end.
- **remaining:** [friction] Prototype async-teardown is still structurally absent. Once C4 rejects the by-value prototype-into-singleton and the user switches to Lookup<Worker>::get_owned(), each per-call owned Worker registers NO destroyer and has no async Drop, so a stateful Worker holding a resource needing close().await leaks per call. This is unchanged from the original friction and is INTENTIONAL (charter no-async-Drop). The patched design converts it from an undiagnosed edge to a loud documented divergence + a leaf-doctor build-warning, so it is now diagnosed, not silent. Not a blocker.; [cosmetic] The C4 fix-hint reads 'convert w: Worker to Lookup<Worker> (fresh per call) or LazyRef<Worker>'. For a PROTOTYPE target, LazyRef<Worker> is the wrong steering: LazyRef yields a shared Ref<T> and is defined as 'OnceCell-cached for singleton targets, NEVER cache for prototype targets' (doc-03 L53/L65/L143); a prototype publishes Published::Owned(Box), not an Arc, so it cannot be returned as Ref<Worker>. The only correct fresh-per-call owned handle is Lookup<Worker>::get_owned().await. The primary suggestion (Lookup) is correct, so this is misleading-but-not-unsound diagnostic text. Inherited verbatim from the original proposed fix-hint; surfaced now that C4 is in force.; [cosmetic] S3 (CandidateRole::Fallback auto-config) stays a full member of Vec<dyn Step> alongside the user's S1/S2 because primary_promote/FallbackDemote is structurally bypassed for Cardinality::Multiple. This is correct arithmetic but diverges from the single-injection @Fallback 'user supersedes default' mental model. The patched seam-3 pins it as a documented invariant (Fallback is a single-injection tie-break only; no meaning for collections). Addressed as documentation; no soundness issue.

## `async-cancel-teardown: background-bootstrap startup (WarmCachePool + SchemaMigra`
- **blockers cleared:** True
- **verdict:** BLOCKERS CLEARED — the patched design composes for async-cancel-teardown. All four original blockers are GONE: (1) no in-flight-request drain → C7/C1 add context-close step 2, a reactive bounded await on RequestScopeRegistry.outstanding→0 (Notify-woken, honoring charter §2.4) between Readiness→RefusingTraffic and stop_all, then cooperative RequestCancel + per-request-ledger drain; the suspended checkout() is found because the web layer registered its scope via enter(). (6) Arc-count conflates memory-free with logical-destroy → resolved by the step-2-before-step-5 barrier: singleton destroyers (DbPool.close()) run in step 5 ONLY after step-2's grace elapsed and stragglers were cancelled-and-drained, so no straggler calls into a closed pool; Arc-soundness is correctly scoped to MEMORY only. (7) abandoned tx → C7 pins the @Transactional begin() to push a TxFinalizeEntry onto the AMBIENT request-scope ledger (reusing InstanceStore::ledger(), no new ledger type); the sync-Drop guard only sets must_rollback, and the registry's step-2 drain runs the awaited rollback — 'no async Drop' holds and the tx is deterministically rolled back, finalize-once via a OnceCell. (4) Refreshing×shutdown undefined → C7/C1's ShutdownRequested{AtomicBool+Notify} flag: a shutdown CAS while RunState∈{Created,Refreshing} sets the flag instead of running (C); refresh() checks it at each R-step boundary and diverts into (B) cancel-cascade; the container ledger's single serialization owner makes R5 pushes and a drain race-free; pre-Running request scopes structurally cannot exist (Readiness flips only post-R8), so the two teardown directions are disjoint. The compound advice+scope axes also hold: C3's two-site proxy rule (after_init wraps ONLY Once/Owned; PerContextKey is wrapped at the injection seam over a ScopeTarget; InstanceStore holds the BARE ErasedBean) means @Cart's tx chain fires exactly once per call and the ScopeTarget re-resolves the live per-request instance; C4's widened ScopeMismatch + AdvisedConcreteInjection (with ProxyPlan as an explicit validate() input) and C6's RoleTier/cmp_chain are consumed cleanly by R2/R4. The forward path (background JoinSet waves, wave-partition invariant, SmartInitializing barrier, SchedulerCore arming at R6 and disarming at shutdown step 1) is coherent. The remaining issues are all FRICTION/COSMETIC doc-reconciliation debt introduced by authoring only 13-container-lifecycle.md while the C5/C7 corrected texts in the prompt refine or diverge from it (single- vs two-budget drain; SpawnHandle-abort vs registry-holds-store-and-drains wording; implicit per-request-ledger single-drain contract; stale events.md). None re-opens a Phase-2 toolkit primitive and none reaches blocker severity for this scenario; they should be landed verbatim into doc 13's API sketch + step-2 prose and into events.md to make the design PRISTINE.
- **NEW:** [friction] The authored doc says surviving request tasks 'are cooperatively cancelled (SpawnHandle abort at the next .await), and each cancelled request's per-request InstanceStore ledger is DRAINED'. SpawnHandle DROP=abort (execution-context line 30) just DROPS the suspended future at its await point, which runs only sync Drop — it does NOT run the awaited TxFinalizeEntry on the per-request ledger. The C7-corrected design (in force) actually closes this by NOT relying on the dead task: the RequestScopeRegistry's TrackedScope holds an Arc<dyn InstanceStore>, so after firing the cooperative RequestCancel watch-signal (distinct from a foreign abort) the REGISTRY itself awaits store.ledger().drain(finalize_budget), running the rollback on the shutdown task. The mechanism is sound under the C7 pin, but the authored prose conflates 'SpawnHandle abort' with the cooperative-RequestCancel path and is silent on WHO awaits the ledger after the task is gone, so a literal implementation of doc 13 line 59 (abort-then-the-dead-task-drains) re-opens the abandoned-tx hole the fix exists to close. Doc-reconciliation gap, not a mechanism contradiction: the rollback still runs because the registry owns the store Arc.; [friction] The authored API exposes a SINGLE grace Deadline covering both the quiesce-await and the per-request-ledger drain. The C7 fix (in force) specifically SPLITS this into two budgets so a wedged DB rollback consumes only finalize_budget and can never hang shutdown indefinitely, recording overruns as finalize_overruns in the ShutdownReport. A reader implementing doc 13's single-budget signature verbatim re-introduces the exact 'a stuck finalizer hangs shutdown' hazard the C7 refinement was authored to remove. The two-budget version is the in-force design; doc 13's sketch is stale relative to the pin and must be reconciled.; [friction] The C1/C7 single-serialization-owner invariant is pinned explicitly only for the ONE container TeardownLedger ('SINGLE owner/serialization point'); it is NOT explicitly pinned for the per-request InstanceStore ledger family. At the grace/finalize boundary a request that cooperatively unwinds to Holder::scope right as finalize_budget elapses could have BOTH its own guard-Drop scope-end drain AND the registry's direct store.ledger().drain() race on the same per-request ledger. The actual tx-corruption hazard is closed by C7's per-entry finalize-once OnceCell<Outcome> on TxFinalizeEntry (idempotent commit/rollback) and by the registry Mutex<SlotMap> serializing slot ownership/removal, so this is bounded — but the per-request-ledger 'who drains, exactly once' contract is left implicit where the container-ledger one is explicit. A friction worth pinning (state that the per-request ledger has the same single-drain discipline, or that double-drain is safe by entry-level finalize-once), not a soundness blocker.; [cosmetic] C5 (in force) replaces the type-false verbatim-Interceptor reuse with a distinct per-listener DispatchInterceptor trait and pins the listener condition to PUBLISH-TIME for both inline and deferred listeners. Only 13-container-lifecycle.md was authored; events.md was not edited and still carries the contradicted claims. For THIS scenario the divergence is non-load-bearing: the only events touched are Closed{reason} (published at shutdown step 3 under IsolateEach, no listeners that fan-out per-listener stress the trait shape) and the tx-deferred @Async drain-set registration, which C5(C) routes into the same facility-drain set the multicaster quiesce drains — and doc 13 line 165 already states that interaction correctly. So the stale events.md does not break the async-cancel-teardown trace; it is a cross-doc reconciliation debt the loop-back must still land in events.md, flagged here for completeness since the @Cart is advised and the Closed dispatch is on this scenario's path.

## `discovery-antidce: three starters; one crate contributes zero rows (genuine DCE-`
- **blockers cleared:** True
- **verdict:** CONVERGES for this scenario — zero blocker-severity breaks remain, and the 6 fixes introduce no new blocker. Traced source->macro->linkme->App<Define->Resolve->Wired>->seal->refresh(R0..R8)->steady-state->teardown. The prior blocker for this scenario family (override+exclude+fallback: 'all-fallback silently wins on vanish') is GENUINELY CLOSED: the patched design runs a CHEAP anchor-presence gate at the Define->Resolve edge (BEFORE App<Wired>::validate()/FallbackDemote), so a genuinely DCE-vanished regular-bean crate is a loud AntiDceError::SourceVanished before FallbackDemote could mis-select the library Fallback; and the C2 R0 four-class classify() (post-ConditionReport, the only site with the report available) provides the headline reconcile. The three required distinctions hold and never conflate on any soundness-critical path: (A) genuine DCE-vanish -> anchor_absent -> SourceVanished(true-DCE) LOUD; (B) condition-not-met -> all-expected-ContractId-Negative -> Negative(silent); (C) cfg-gated -> CompiledOutByCfg, correctly handled at two granularities — a whole capability-gated crate is simply absent from EXPECTED (never classified, no misfire), and a capability-feature-gated component is labeled per-element by the ConditionReport finalize fusion; (D) user dyn Cache (Normal) vs auto-config Fallback CacheManager -> FallbackDemote STEP A drops the Fallback so the user bean wins Unique. The validate-before-R0 timing split (C2) does NOT open a silent-wrong-winner hole because the pre-validate cheap anchor gate already covers the only LOUD case (true DCE); the remaining ways a regular bean 'disappears' leaving a Fallback to win (cfg-opt-out, condition back-off) are the CORRECT outcomes and are inspectable. Two friction/cosmetic residuals persist, both PRE-EXISTING (not introduced by the patch): the anchor-survival contract underpinning classify()'s anchor_absent-first ordering is un-pinned (could yield a loud wrong-reason SourceVanished for an all-cfg-off participating crate — the line-83 risk, only partially mitigated by the new clause (3)), and a crate-local (non-capability) cfg gate produces no inspectable CompiledOutByCfg label (sound but diagnostically thin). Recommend pinning, in discovery-codegen, that a participating crate's provides_descriptors! anchor is emitted UNCONDITIONALLY (cfg-independent of its beans) so classify()'s predicate order is provably sound, and documenting that crate-local feature gates degrade to NoSuchBean/silent-absent rather than CompiledOutByCfg.
- **remaining:** [friction] Anchor-survival contract is un-pinned, leaving the classify() predicate order soundness-conditional. classify() lists anchor_absent->SourceVanished(true-DCE) as predicate (1), BEFORE CompiledOutByCfg as predicate (3). This is only correct if a participating crate's provides_descriptors! anchor is emitted UNCONDITIONALLY (cfg-independent of its individual beans). The docs never state this. If a participating crate whose every bean is cfg-gated-out also loses its anchor (e.g. the anchor is auto-emitted by the same macro that emits a bean), predicate (1) fires first and a CompiledOutByCfg crate is reported as SourceVanished(true-DCE) — a loud WRONG-REASON diagnostic (DCE-bug witch-hunt for a deliberate feature-off). This is exactly the conditions-autoconfig line-83 risk; the new four-class classify() partially mitigates it via clause (3) but only when the anchor survives, so the anchor-survival rule is the load-bearing detail the patch left implicit.; [cosmetic] A component gated by an ARBITRARY CRATE-LOCAL cargo feature (not a leaf umbrella capability feature) that is OFF produces no CompiledOutByCfg report row: the CapabilitySnapshot tracks leaf capability features, not arbitrary crate-local features, and the ExpectedManifest is per-SourceTag/per-crate row counts, not per-component ContractIds. Behavior stays SOUND (mandatory injection of the absent component -> loud NoSuchBean at validate; unused -> correctly silent), but the inspectable CompiledOutByCfg label the scenario wants (so a feature-off component never reads as a vanish or a back-off) is only produced for leaf-capability-feature gates, not for plain crate-local feature gates. Diagnostic-richness gap, not a soundness/conflation break.; [cosmetic] Stale doc prose: 09 still says the advisor chain sorts by '(RoleTier, OrderKey, ContractId) ... the canonical leaf_core::order::cmp_order, no second comparator', but the C6 loop-back pins cmp_chain as the RoleTier-first composite precisely because cmp_order alone cannot see RoleTier. 13-container-lifecycle and the C6 seam are the binding design (cmp_chain). Does not affect the discovery-antidce scenario (which exercises FallbackDemote selection of the user dyn Cache, not the CacheManager advisor chain order), but the un-updated prose is a residual doc/seam drift.

## `config-binding-path: env(DB_POOL_SIZE=0) → relaxed-binding → @ConfigurationPrope`
- **blockers cleared:** False
- **verdict:** PARTIAL. Of the scenario's two original blockers, exactly ONE is genuinely cleared and one is NOT. CLEARED — the RoleTier-definition-gap blocker: cluster-2 supplies leaf_core::order::RoleTier{Infrastructure=0,Support=1,Application=2} with const fn of(Role), the shared ChainKey, and cmp_chain (RoleTier ASC → pure cmp_order → ContractId) while keeping cmp_order itself unchanged; doc-13 R2/R3/R6/R7 now call cmp_chain, so the infra bring-up ordering the scenario needs at refresh is expressible. The order-table is internally consistent (CACHE=400<TX=500, TRANSLATE=600=max, ASYNC_DISPATCH=100<ERROR_ISOLATION=200), and since DbProps/PoolFactory are un-advised the table is inert for this trace — no new break from it. NOT CLEARED — the config-properties bind+validate LOCUS blocker (the scenario's headline question): this blocker is simply not among the 6 fix clusters. It survives only as an unpinned ripple note + risk bullet inside doc-13, while the two OWNING subsystems (binding-conversion 07, injection-resolution 03) still carry the original three-way contradiction, and doc-13's own normative R5 eager-bitset (line 38) contradicts the ripple note's 'eager-excluded-because-prebound' claim. So for DB_POOL_SIZE=0 the range(min=1) failure still has an undetermined surfacing tier (Tier-2 aggregated report vs R5 cancel-cascade Failed), and the dependent Skip-never-elides-config-validation blocker is likewise unpinned. The good news matches the prior trace: the happy-path data flow (relaxed-binding env override → CPS adapter → FromConfigValue Duration → tree-descent for Vec/nested → Validate-after-each-Object → env-var origin) is coherent and the cluster-3/4 ScopeMismatch+AdvisedConcrete additions are correctly inert here. But the PRISTINE bar is not met: a second Phase-3 loop-back is required to pin config-properties as the one bean class validate() materializes (with R5's eager bitset edited to exclude pre-bound config beans, doc-07/doc-03 reconciled, the @Value-coercion timing and Skip carve-out folded into the strictness lever, and the env-var Origin threading + Vec-under-non-enumerable collection-owner rule closed).
- **remaining:** [blocker] NOT closed by the 6 provided fix clusters. The config-properties bind-locus fix appears NOWHERE in the cluster texts I was told to apply as the current design (cluster 1's R0..R8 has no pre-bind step; validate() inputs are pinned only as FrozenContainer+WiringPlan+ProxyPlan). It survives ONLY as an unpinned assertion in doc-13: a one-line environment-config RIPPLE note (line 168, 'Config-properties beans are pre-bound INSIDE App<Wired>::validate()... R5 publishes their already-bound Arc, eager-excluded-because-prebound') and a remaining-RISK bullet (line 184). The normative specs still contradict it three ways: (a) doc-07 still states binding is a Provider::provide ready-future that runs at App<Resolve>→App<Wired> AND aggregates Tier-2 — but Provider::provide only runs at R5 per the very template doc-13 authored; (b) doc-03 line 154 still says validate() runs resolve_one 'before any constructor' with NO carve-out for the one bean class validate() must now materialize; (c) doc-13's OWN R5 (line 38) puts every non-lazy/non-scoped singleton (DbProps is Once/Application) in the eager bitset and calls Engine::create → Provider::provide, with no 'eager-excluded-because-prebound' carve-out — so R5 would re-bind, double-running the bind and routing a failure into the R5 cancel-cascade (RunState→Failed/StartupFailed) instead of the promised Tier-2 aggregation. For DB_POOL_SIZE=0 the range(min=1) violation therefore lands at an undetermined tier: either validate() (only if the unpinned pre-bind pass is real) or R5 cancel-cascade (per the actual R5 spec) — the headline 'one aggregated startup report, fail-before-construction' guarantee is not pinned for the most common externalized-config failure.; [blocker] The scenario's sibling blocker (Skip silently dropping the range/nested JSR checks, which have no Tier-3 fallback) is addressed ONLY as a doc-13 remaining-RISK bullet (line 184: 'Skip must still NOT elide config-properties validation... co-owned with the strictness lever') — it is co-owned and asserted, not pinned into the lever's own definition. The 6 fix clusters do not touch the strictness lever. The Skip carve-out for config-properties is parasitic on the unclosed bind-locus fix above (it presupposes config-properties is a construction step that always runs strict); since that fix is itself unpinned, this one cannot be considered closed either.; [friction] Unchanged by the patch set. The lever is a 3-state enum {Strict,Lenient,Skip} threaded 'the way Leniency is threaded', but the binder/convert layer consumes a 2-state Leniency{Strict,Lenient}; the Strict→Strict / Lenient→Lenient / Skip→Strict collapse is still undocumented at the validate() head. Pre-existing friction, not a blocker for the DB_POOL_SIZE=0 failure (which fails under default Strict), but still open.; [friction] Unchanged by the patch set. The range(min=1) Violation carries field path 'pool_size' + rejected '0' but no OriginId; the join back to the env-var origin (DB_POOL_SIZE) depends on an unpinned field-path↔OriginId map shared between the bind pass and the separate validation pass. The scenario's promised diagnostic ('range(min=1) for db.pool-size=0, env var DB_POOL_SIZE') is therefore not guaranteed to name the origin. Pre-existing friction.; [friction] Unchanged by the patch set. With OsEnv (non-enumerable, answers Unknown for contains_descendant_of(db.hosts)) sitting ABOVE the file's Present answer, it is still unpinned whether the file correctly owns the whole hosts list or a four-candidate probe of DB_HOSTS_0_HOST could blend/wrongly-own it (never-blend hazard). Pre-existing friction the scenario directly exercises.
- **NEW:** [blocker] The C1 loop-back authored doc-13 with an internal self-contradiction it introduced: the ripple note claims config-properties are 'eager-excluded-because-prebound' and R5 'publishes their already-bound Arc', but R5's normative text (line 38) defines the eager bitset as excluding only 'lazy/scoped/prototype' and force-including smart-init + Role::Infrastructure — a singleton Application config bean (DbProps) matches none of the exclusions, so per the literal R5 spec it is eager-instantiated via Engine::create→Provider::provide. The note and the step disagree about whether R5 re-binds config beans. This is a fix-introduced inconsistency (the prior design had the contradiction across subsystems; the patch moved a claim into doc-13 without reconciling doc-13's own R5 bitset definition).; [friction] The fix asserts the bind-locus resolution is 'co-owned with binding-conversion', but binding-conversion (07) and injection-resolution (03) were not edited to match, so the cross-subsystem ownership hole the original blocker named (no single subsystem owns the materialize-config-at-validate decision) is reproduced rather than closed: doc-13 says one thing in a ripple, doc-07/doc-03 say the contradictory thing in their normative bodies. This is a friction the loop-back created by documenting the fix in only one of three places that must agree.

## `events-tx-async: a @Transactional OrderSvc.place() publishes OrderPlaced; a sync`
- **blockers cleared:** True
- **verdict:** PRIOR BLOCKERS GONE; scenario composes. All three prior blockers for events-tx-async are genuinely closed by the patched design: (#1 verbatim proxy-Interceptor reuse) C5 mints a structurally-distinct per-listener `DispatchInterceptor::around(ctx, entry, next: ListenerNext)` whose `ListenerNext::invoke` is forward-only (vs the proxy `Next::proceed` REPLAYABLE+SKIPPABLE shape verified in 08), the per-listener fan-out lives in CoreDispatch, and it shares ONLY cmp_chain + the RoleTier grade with proxy-substrate — the type-impossibility is resolved on the fixed `aop::Interceptor` (Call→ErasedRet) primitive without reopening Phase 2. (#2 @Async spawn-site) C5 part C pins defer=Some as structurally ineligible for AsyncDispatch and makes the tx-sync AFTER_COMMIT callback own the spawn at the phase boundary, with CoreDispatch always reaching the defer branch before any chain hop — L2 fires exactly once at commit with the tx Isolate key correctly dropped and Inherit keys riding; no double-fire, no pre-commit run. (#3 condition timing) C5 part E picks declarative-advice's publish-time contract for BOTH inline and deferred listeners and corrects events.md line 34/40 — one contract. The four prior frictions: three are fully resolved (cross-cohort @Order → partitioned ListenerSeq{inline, deferred:BTreeMap<TxPhase,..>} each cmp_chain-sorted + doctor diagnostic; Box-vs-Arc adapter ABI → ErasedEvent.value:Arc<dyn Any+Send+Sync> with one unified adapter; sync-listener-Err-rolls-back-tx → documented coupling + doctor note + refresh-time validation). The supporting fixes are well-grounded: C6 supplies the previously-missing RoleTier enum + cmp_chain composite (the 08/09 'sort by (RoleTier, cmp_order, ContractId)' prose had no callable function and `cmp_order` had no RoleTier param) and — as a real latent-bug fix — corrects the internally-contradictory declarative-advice line-154 chain list (which placed CACHE inside TX, violating its own 'cache hit avoids tx' invariant) to CACHE(400)<TX(500); not on this scenario's `[tx]`-only chain, but its multicaster sub-table (ASYNC_DISPATCH 100 < ERROR_ISOLATION 200) correctly enforces the events line-176 'AsyncDispatch outside ErrorIsolation' requirement. C7's TxFinalizeEntry-on-the-per-request-ledger + two-budget in-flight drain soundly closes the abandoned-tx-rollback hole using the real InstanceStore::ledger() seam (verified in 04). The ONLY residual is friction, not a blocker: the tx-deferred @Async-spawn-vs-teardown ORDERING (STRESS-TESTS line 250) is asserted closed but its mechanism contradicts the doc's own LIFO=reverse-publication drain — the infrastructure facility-quiesce (early-published) drains LAST while the late-published @Async host destroyer drains FIRST, and the detached L2 escapes the step-2 request-scope count; nothing hoists the facility-quiesce ahead of the host destroyers, so a straggler L2 can touch a logically-closed host in the shutdown window (memory-safe via Arc, so logical-teardown only). Plus a cosmetic doc-vs-fix drift on the drain() signature (single-budget in the authored doc, two-budget in the C7 fix). No NEW blocker introduced by the fixes. Recommendation: pin the facility/multicaster-quiesce as an explicit pre-LIFO step in shutdown step 5 (or include detached @Async SpawnHandles in the step-2 outstanding count), and reconcile 13-container-lifecycle.md's drain() signature to the C7 two-budget form.
- **remaining:** [friction] The fix ASSERTS the tx-deferred @Async L2 spawn is drain-safe because its SpawnHandle is registered into the facility/multicaster-quiesce drain set and 'host-bean destruction is ordered after that drain'. But the mechanism contradicts the stated order. (a) L2 is fire-and-forget Detach: the request task spawns it at commit and returns WITHOUT awaiting it, so the RequestScopeGuard drops and the step-2 in-flight-request drain reaches outstanding==0 while L2 is still running detached — step 2 (which counts request SCOPES) does not cover L2. (b) L2's safety therefore rests entirely on the facility/multicaster-quiesce ledger entry running BEFORE L2's host-bean destroyer in step 5. But step 5 is a pure LIFO = reverse publication = reverse wave-order, and the multicaster/ExecutionFacility are Role::Infrastructure (early-published → drained LAST), while L2's host is an Application bean (late-published → drained FIRST). So under the stated LIFO the host destroyer runs BEFORE the quiesce that awaits L2 — the exact inversion the claim forbids. No pre-LIFO hoist of the facility-quiesce entry, and no inclusion of detached @Async spawns in the step-2 outstanding count, is specified. Result: in the shutdown-race window L2 can touch an already-logically-destroyed (close()'d) host bean. The toolkit's Arc-strong-count keeps it memory-safe (L2 holds an Arc clone of the host), so this is a logical-teardown ordering hazard, not UB — which is why it was originally rated friction, but the fix overstates it as closed.; [cosmetic] The authored doc ships the C1-sketch single-budget form: `drain(grace: Deadline) -> DrainOutcome` and `DrainOutcome { Quiesced, CancelledAfterGrace { drained, ledgers_run } }` (lines 134-137). The C7 fix explicitly supersedes this with a two-budget `drain(grace, finalize_budget)` and `CancelledAfterGrace { drained, ledgers_run, finalize_overruns }` plus `BootstrapSettings.shutdown.finalize_grace`, so a wedged DB rollback consumes only finalize_budget and cannot hang shutdown. Applying the fix as authoritative, this is a doc-vs-fix drift, not a design defect — but the doc text and the fix diverge on a load-bearing signature and would mislead an implementer reading 13-container-lifecycle.md alone.

## `diagnostics-tiers: NoUniqueBean ambiguity (F1) + missing mandatory dep via genui`
- **blockers cleared:** False
- **verdict:** NOT PRISTINE for this scenario; blockers NOT cleared. The prior assessment's two GOOD findings hold and are now even more solidly backed: F1 (NoUniqueBean) walks seam #3's FallbackDemote->PrimaryPromote->name_match fold exactly (Fallback_C demoted, {Normal_A,Normal_B} -> loud NoUniqueBean at Tier-2), and F2 (missing dep) walks the anti-DCE 4-class distinction perfectly (force-linked, >=1 row, OnProperty Negative -> NoSuchBean at Tier-2, enriched from ConditionReport). Both land in the one Tier-2 AssemblyReport. The C6 RoleTier/cmp_chain and C2-FallbackDemote pins are real and consumed. BUT the scenario's central promise — all three surface TOGETHER in ONE report — still FAILS, for the SAME reason the original test flagged: the fix for it lives in cluster C2 (the throwaway @Value dry-run sub-pass at validate, plus the registry-core validate-placement edit, plus the env-06/binding-07 strict reconciliation, plus the BootstrapSettings 8th field), and C2 was NOT one of the 6 applied loop-backs (C1/C3/C4/C5/C6/C7). The 6 fixes that WERE applied are clean for this scenario and introduced no scenario-breaking new defect — the C1 doc correctly handles this scenario's pre-refresh teardown, and the only new issue is a harmless doc-internal C7 drain-signature mismatch that this scenario never exercises. Net: the breaks this scenario exposes are precisely the ones the convergence team explicitly deferred (C2), so they are EXPECTED-but-still-open. Recommend applying the C2 doc-edits (validate-time @Value dry-run pinned in injection-03 + binding-07; registry-core-01 refresh template edited to validate-before-refresh; env-06 'lenient @Value' line deleted; BootstrapSettings.startup_validation field added) before this scenario can be called pristine. Two genuine blockers remain (@Value Tier-3 masking; registry-core validate-placement contradiction); four frictions remain; one harmless new friction introduced.
- **remaining:** [blocker] PRIOR BLOCKER #1 NOT CLEARED. The @Value coercion fracture is intact. The fix that was prescribed for this scenario (STRESS-TESTS break #1 / CONVERGENCE C2: add an explicit throwaway @Value DRY-RUN resolution sub-pass at validate() that calls resolve_into::<T> / from_config_value and DISCARDS the value to collect Convert errors at Tier-2) was NOT applied — C2 is not among the 6 applied loop-backs (REPORT.md lists only C1/C3/C4/C5/C6/C7; C2 is named a 'reconciliation' but deferred to doc-edits that were never made). binding-07 still says the coercion runs only inside Provider::provide at construction (refresh R5 = Tier-3); injection-03's validate pass still short-circuits PointKind::Value and aggregates only wiring failures; no implementing doc pins a validate-time dry-run. Only the SEAMS seam #5 RIPPLE prose (line 118) vaguely says validate() 'builds [a ConvertCtx] for eager @Value resolution', which is ambiguous and does not override the two doc bodies. CONSEQUENCE for this exact scenario: F1+F2+F3-configprops aggregate at Tier-2 and abort BEFORE refresh; F3-@Value (`abc`->u16) is never exercised at validate, surfaces only on a SECOND startup at Tier-3 after the first three are fixed. The scenario's central promise 'all surface together in ONE report at ONE tier' still fails.; [blocker] PRIOR BLOCKER #2 only HALF-cleared. The stub half is fixed (C1 authored 13-container-lifecycle.md, which correctly pins validate-BEFORE-refresh and the pre-refresh 'nothing constructed, builder dropped, RunState=Created' teardown path this scenario hits). But the validate-PLACEMENT contradiction persists: registry-core-01 STILL states validate() runs INSIDE refresh AFTER context-services install, directly contradicting the patched 13-doc, bootstrap-diagnostics-14 (line 15/34: seal()->validate()->refresh), and seam #1. The seam's own C1 ripple text says 'registry-core line 32's inline refresh-order prose defers to THIS template' — but registry-core-01 on disk was never edited. The two readings give DIFFERENT teardown obligations and DIFFERENT RunState-at-failure (inside-refresh => Refreshing + ledger drain of installed infra beans; before-refresh => Created + plain builder drop). For THIS scenario the failure is genuinely pre-refresh so the user-visible outcome is correct under the 13-doc reading, but the cross-doc disagreement is unresolved and load-bearing for any reader who follows registry-core.; [friction] PRIOR FRICTION NOT CLEARED. env-config-06 still asserts @Value reads are LENIENT-by-default, directly contradicting binding-conversion-07 ('STRICT framework default, an unresolved placeholder is a Tier-2 UnresolvedValue node, never a silently-injected literal') and SEAMS seam #5 (Strict resolves every @Value with Leniency::Strict). The prescribed fix (delete 'lenient @Value reads for parity', replace with the strict-collect-into-AssemblyReport wording) was a C2 doc-edit and was not made. In a variant where `order.max-retries` were ABSENT rather than uncoercible, the env-06 reading would silently inject the literal and mask F3 entirely.; [friction] PRIOR FRICTION NOT CLEARED. BootstrapSettings on disk is still the 7-field CLOSED type {banner_mode, web_application_type, lazy_initialization, allow_bean_definition_overriding, allow_circular_references, register_shutdown_hook, keep_alive}. The 8th field `startup_validation: StartupValidation` that seam #5 mandates (the lever read at validate()'s head that gates Strict/Lenient/Skip for this scenario's aggregation) is NOT added to the struct. The config key/shape inconsistency (`leaf.main.startup-validation` 3-state enum vs `leaf.validation.strict` bool) also persists across the seams. The lever the whole 'surface together' story depends on is unbacked by the type that must carry it.; [friction] PRIOR FRICTION NOT CLEARED. Enrichment-from-ConditionReport/ExpectedManifest prose is still specified ONLY for NoSuchBean ('backed off because/feature X not enabled/crate expected but zero rows'). For F1's NoUniqueBean over {Normal_A, Normal_B} (after Fallback_C is correctly demoted), no doc pins the join that would tell the user WHICH survivor is the unexpected one (the stray force-linked test-double crate). The genuinely actionable insight for this scenario is still under-specified.
- **NEW:** [friction] INTERNAL INCONSISTENCY introduced by the C7 loop-back being only partially propagated into the C1-authored doc. The C7 fix mandates `RequestScopeRegistry::drain(grace: Deadline, finalize_budget: Deadline)` (two budgets) and `DrainOutcome{Quiesced, CancelledAfterGrace{drained, ledgers_run, finalize_overruns}}` plus `BootstrapSettings.shutdown.grace` AND `.finalize_grace`. But the 13-doc still shows the C1-era single-budget `drain(&self, grace: Deadline)` and `DrainOutcome{Quiesced, CancelledAfterGrace{drained, ledgers_run}}` (no finalize_overruns), and references only `BootstrapSettings.shutdown.grace`. This is harmless for the diagnostics-tiers scenario specifically (its failure is pre-refresh, so the in-flight drain is never reached), but it is a real new doc-internal divergence the patch introduced. Flagged as friction, not a blocker FOR THIS SCENARIO.

