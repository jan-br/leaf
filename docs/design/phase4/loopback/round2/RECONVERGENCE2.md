# Loop-back Round 2 — Re-convergence

## `config-binding-path with round-2 fixes applied and verified on disk`
- blockers cleared: True
- verdict: PASS for config-binding-path. Zero blocker-severity breaks for this scenario and no new break introduced. Verified on disk: C2 config-properties bind plus JSR locus pinned to the ONE site validate (binding-07 items 1 and 2 plus line 43): pre-materialize via pure-projection binder-backed Provider, ValidationBindHandler runs Validate after each Object node, aggregate to a Tier-2 AssemblyReport, store the bound Arc in the slot OnceCell. DB_POOL_SIZE=0 (relaxed to db.pool-size) failing range min 1, with nested Object descent, Vec via List of Scalar, and Duration via the FromConfigValue suffix grammar, surfaces at Tier-2 validate while RunState is pre-refresh. The @Value dry-run is pinned (injection-03 lines 21 and 154; binding-07 dry_run_value line 122 plus the per-bean dry-run thunk line 123); db.region resolves to us-east via the same monomorphized resolve_into. The R5 self-contradiction is closed across 13-doc line 9 normative bitset override, SEAMS seam-1, registry-01 lines 32 and 135, and binding-07: a pre-bound config bean is never re-run at R5, so no cancel-cascade. Validate-placement reconciled; env-06 lenient-for-parity line deleted (lines 40 and 42) with residual hits only in prior-round analysis docs. Charter 1.7 honored: config JSR has no Tier-3 fallback and stays HARD under Lenient and Skip. The SEAMS C6 stub is the sole residual and is friction-only for this scenario.</verdict>

  - remaining [friction]: C6 seam still the literal placeholder stub on disk; round-2 payload claimed a corrected RoleTier/cmp_chain text but the edit was never applied. Not load-bearing on config-binding-path because cmp_chain is used only at refresh R2/R3 Infrastructure ordering, and DbProps is an Application-role pure-projection bean pre-bound at validate and excluded from R5; the range Tier-2 promise holds regardless. Friction here; a blocker for proxy/advice/events scenarios and the leaf-core ABI freeze.

## `diagnostics-tiers: NoUniqueBean (F1) + missing-mandatory-dep-via-genuine-Negativ`
- blockers cleared: True
- verdict: PRISTINE for the diagnostics-tiers scenario: blockersCleared=true, ZERO blocker-severity breaks remain, and no new break is introduced ON this scenario's path. Full trace (source → macro/linkme → App<Define→Resolve→Wired(validate)> → seal/refresh) verified end-to-end against the on-disk docs, which DO carry the round-2 fixes for every doc this scenario touches. (1) The C2 config-locus fix is landed in all owning docs: injection-03 (shared-machinery item 6 + the compile-runtime-split interaction + public-API dry_run_value), binding-07 (config-properties at validate with R5 PREBOUND exclusion + value-injection validate-time throwaway @Value dry-run + the strict-default error spine), env-06 (the 'lenient @Value reads for parity' line DELETED and replaced with strict-collect + validate-time dry-run at lines 40/42/184), and 14-bootstrap (BootstrapSettings is now the 8-field record with startup_validation: StartupValidation + shutdown: ShutdownSettings; the StartupValidation{Strict,Lenient,Skip} enum + App::set_startup_validation present). (2) The R5 self-contradiction is RESOLVED: 13-container-lifecycle line 9 pins the eager bitset as excluding lazy/scoped/prototype AND the pre-bound config beans (eager-EXCLUDED-because-PREBOUND), force-including only smart-init + Role::Infrastructure, so a malformed config value can never route into the R5 cancel-cascade. (3) Validate-placement is reconciled: 01-registry line 32 + line-135 refresh() comment now state validate-BEFORE-refresh (R0 = anti-DCE row-count reconcile, NOT a pre-validate step), consistent with 13-doc, SEAMS seam-1, and 14-bootstrap step-10/11. (4) The strictness lever (SEAMS lines 108-118) is fully authored and folds in the config-properties JSR carve-out (range/nested checks stay HARD under Lenient AND Skip — no Tier-3 fallback — honoring charter §1.7). SCENARIO RESULT: at App<Wired>::validate() (default Strict, read once at head), F1 NoUniqueBean (Selector len>1 terminal rule), F2 NoSuchBean enriched via report.lookup(contract)→'backed off because…' (the conditions-05 silent-Negative/loud-later bridge, kept distinct from anti-DCE SourceVanished), F3a Convert (order.max-retries=abc→u16 via the throwaway @Value dry-run), and F3b BindError/Violation (order.timeout=5quux via config-properties pre-materialization) ALL aggregate collect-all (not fail-on-first; 'fails fast all-or-nothing into one AssemblyReport') into ONE Tier-2 AssemblyReport BEFORE refresh — exactly the scenario's central promise. The only on-disk defect found is the un-applied SEAMS C6 RoleTier stub (recorded as a newBreak at friction severity): it is a genuine corpus-consistency gap that contradicts the round-2 'verified in every owning doc' claim and blocks the leaf-core ABI freeze and the cmp_chain/advice-ordering scenarios, but it is provably OFF the diagnostics-tiers F1/F2/F3 path, so it introduces no blocker for THIS scenario.
  - NEW [friction]: The C6 'RoleTier plus order table' seam is STILL the literal placeholder stub on disk (Decision: 'Define RoleTier and a cmp_chain' / Affects: 'a, b' / Rationale: 'x; y' / Ripples: 'm; n'). The round-2 fix payload's reconciliationEdits explicitly claimed this section was rewritten to the full RoleTier enum + cmp_chain/ChainKey + order:i32 table content; that edit was NOT applied to disk (the round-2 docs touched only 01/03/06/07/13/14 — mtimes 22:53-22:57 — while SEAMS C6, 08-proxy, 09-advice, 12-events retain their 20:46 pre-fix state). This is a real corpus-consistency defect and contradicts the round-2 'markers verified present in every owning doc' claim. NOT on the diagnostics-tiers F1/F2/F3 path (that path uses the Selector layer-fold + cmp_order at PriorityRank, the ConditionReport silent-now/loud-later bridge, and the binder/@Value coercion — none of which traverse cmp_chain/RoleTier; 07-binding and 14-bootstrap have ZERO cmp_chain/RoleTier mentions, 03 has only the unchanged cmp_order line), so it does not break THIS scenario — but it blocks the leaf-core ABI freeze and the C5/C6-dependent scenarios. Severity friction for diagnostics-tiers; would be a blocker for the advice-ordering / RoleTier scenarios.

## `async-cancel-teardown: background-bootstrap startup (R5 waves) + request-scoped `
- blockers cleared: False
- verdict: NOT PRISTINE — blockersCleared=false. The C2 cluster IS landed and internally consistent: the validate-time materialization (config-properties pre-bind + JSR at validate, the throwaway @Value dry-run, R5 eager-EXCLUDED-because-PREBOUND), the validate-BEFORE-refresh placement, the env-06 strict-collect rewrite, and the 8th BootstrapSettings.startup_validation field are all present and agree across 01/03/06/07/13/14. Under C2, C1 and C7 DO hold for the scenario's core: a malformed config (DB_POOL_SIZE=0 vs range(min=1); order.max-retries=abc->u16) is a Tier-2 AssemblyReport at validate() with RunState still pre-refresh, NEVER an R5 cancel-cascade Failed; and the SIGTERM in-flight drain (13-doc teardown step 2: RequestScopeRegistry bounded grace -> cooperative-cancel -> per-request InstanceStore ledger drain so the tx Isolate rollback runs; step 5 gates singleton destroyers after the grace) is correct. So 7 of the 10 round-2 reconciliationEdits are applied and the C1/C2/C7 re-confirmation succeeds AS A COMPOSED TRACE.

But the re-stress was adversarial and the bar is PRISTINE (zero blocker-severity breaks AND an internally-consistent corpus), and exactly the three C6/C5/C3 propagation edits the round-1 VERDICT flagged as AXIS-2 were NOT applied to disk: SEAMS C6 'RoleTier plus order table' is still the literal stub ('Define RoleTier and a cmp_chain / a,b / x;y / m;n'); 08-proxy still names after_init as the SOLE install seam with no two-site rule and uses cmp_order not cmp_chain; 12-events still claims verbatim aop::Interceptor reuse with no DispatchInterceptor/ListenerNext; and 09-declarative-advice still ships the cache-INSIDE-tx canonical chain (line 154) plus cmp_order (line 15) and an unpinned per-request tx-finalize ledger (lines 19/26). These are load-bearing on THIS scenario's fourth path — the request-scoped advised @Cart spanning .await: a builder reading the owning docs (08/09/12) would build the double-proxy/raw-handoff and the type-impossible verbatim-multicaster, and the chain order around @Cart is governed by a cmp_chain/RoleTier ABI that does not exist anywhere on disk. The composition holds ONLY because the fixes are asserted in-force and the 13-doc carries the correct two-site/R5 prose; the corpus does not agree with itself, so the PRISTINE bar is not met. 4 blocker-severity breaks remain (all on the @Cart-advised + chain-order sub-path), and 1 new friction was introduced (the 14-doc two-budget ShutdownSettings landed ahead of the deferred C7 two-budget drain, leaving finalize_grace unbacked by any drain signature in 13/FIXES). A further loop-back must author the C6 seam + propagate C3/C5/C6/C7 into 08/09/12 (and reconcile the two-budget drain) before this scenario reaches pristine.
  - remaining [blocker]: The C6 seam is STILL the literal placeholder stub on disk: 'Decision: Define RoleTier and a cmp_chain / Affects: a, b / Rationale: x; y / Ripples: m; n'. The round-2 reconciliationEdit prescribing the full RoleTier enum + cmp_chain/ChainKey composite + the fixed order:i32 table (CACHE_ORDER=400 < TX_ORDER=500, etc.) was NOT applied. The scenario's advised @Cart chain order (tx/translate placement around the scoped bean) and refresh R2/R3/R6 Infrastructure ordering are governed by cmp_chain, which has no definition anywhere on disk. leaf-core's pre-1.0 ABI (RoleTier/ChainKey/cmp_chain) — referenced as in-force by 13-doc R2/R4/R6, 08, 09, 12 — cannot freeze while this is a stub. Identical residual blocker the round-1 VERDICT named (AXIS-2: 'SEAMS.md C6 RoleTier+order table still the literal placeholder stub').
  - remaining [blocker]: 08 — the OWNING doc for the proxy install seam the scenario's request-scoped advised @Cart depends on — was NOT reconciled. Item 2 still names after_init as the SOLE 'ErasedBean->ErasedBean swap inside Engine::create' install seam with ZERO mention of the C3 two-site rule (PerContextKey proxied at the injection/resolver seam over ScopeTarget; InstanceStore holds the BARE ErasedBean; after_init SKIPPED for PerContextKey+advised). A builder building from 08 installs the @Cart proxy unconditionally at after_init AND needs a re-resolving ScopeTarget at the injection seam = the double-proxy / raw-shorter-scoped-handoff the C3 fix exists to kill — the exact hazard on this scenario's @Cart-spanning-.await path. Item 5 still sorts by '(RoleTier, OrderKey)' invoking cmp_order, which structurally cannot see RoleTier; it must resolve to cmp_chain. Zero occurrences of TWO-SITE/two-site/cmp_chain/ChainKey/AdvisedConcreteInjection in 08. (The 13-doc R4 carries the correct two-site rule, so the composed trace holds, but the corpus is internally inconsistent — fails the PRISTINE internal-consistency bar.)
  - remaining [blocker]: 12-events STILL claims it 'REUSES leaf_core::aop::Interceptor verbatim' / 'no new comparator/around-advice trait' and the PipelineMulticaster chain is 'Box<[Arc<dyn leaf_core::aop::Interceptor>]>'. This is the exact type-impossible C5 claim: the advice Interceptor wraps ONE Call->ErasedRet while the multicaster must fan OUT over N listeners. The round-2 reconciliationEdit rewriting lines 124/159/166 to the C5 DispatchInterceptor + ListenerNext design (sharing only cmp_chain + the RoleTier grade) was NOT applied — zero occurrences of a standalone DispatchInterceptor trait, ListenerNext, or cmp_chain (the 'DispatchInterceptor' substring hits are only inside AsyncDispatchInterceptor etc.). Dispatch concerns still 'ordered by cmp_order' not cmp_chain. A builder following 12 takes the type-impossible verbatim-reuse path.
  - remaining [blocker]: Three un-applied C6/C7 propagations into the owning advice doc, all on the scenario's advised-@Cart path: (1) line 154 STILL lists the canonical chain order 'VALIDATE -> RETRY -> @ASYNC -> TX -> CACHE -> TRANSLATE' which places CACHE INNERMOST OF (inside) TX, directly self-contradicting the same line's stated invariant 'a hit avoids opening a tx / caching outside tx' — the latent contradiction C6 (CACHE_ORDER=400 < TX_ORDER=500) was meant to fix; (2) line 15 still sorts by '(RoleTier, OrderKey, ContractId)' via cmp_order 'no second comparator', not cmp_chain; (3) C7's per-request tx finalization is not pinned: lines 19/26 say the async tx rollback rides 'an awaited TeardownLedger entry' without naming the per-request InstanceStore ledger (TxFinalizeEntry) that the 13-doc teardown step-2 in-flight drain actually drains — so the scenario's 'per-request tx finalization on SIGTERM' has no home in the owning advice doc.
  - NEW [friction]: The C2 round-2 edit landed the two-budget ShutdownSettings { grace, finalize_grace } onto the 14-doc BootstrapSettings (and the C2 payload explicitly defers the C7 two-budget drain reconciliation: 'finalize_grace deferred to the C7 two-budget reconciliation — referenced, not authored here'). But 13-doc teardown step (2) and FIXES.md still describe a SINGLE-budget drain(grace: Deadline) -> DrainOutcome{Quiesced, CancelledAfterGrace{drained, ledgers_run}} with no finalize_budget arm. For THIS scenario (SIGTERM mid-request with an open @Transactional tx that must be finalized within its own budget after the request-body grace elapses), the two budgets named on disk (grace for body-drain, finalize_grace for tx-finalize) are unbacked by any drain signature — the per-request tx-finalize budget is declared in 14 but has no consuming mechanism in 13. This is a doc-drift friction introduced by landing the 14 field ahead of the deferred C7 two-budget drain authoring, not a correctness regression in the asserted-in-force mechanism (13-doc step-2 still drains the tx rollback under the single grace).

