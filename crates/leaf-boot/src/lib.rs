//! `leaf-boot` — Assembly engine + run pipeline: App<Define→Resolve→Wired→Running>, seal(), refresh/teardown template, run_autoconfig ladder, App<Wired> validation, exit-code coordinator. Force-links leaf-tokio by default.
//!
//! Skeleton crate. Implementation lands per the design corpus in `docs/design/`
//! (phase3 subsystem docs + phase2 `TOOLKIT.md`), built kernel-first.
