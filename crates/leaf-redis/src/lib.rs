//! `leaf-redis` — Representative ecosystem integration: RedisAutoConfig (Fallback) + Infrastructure Providers. Depends on leaf-core (+ a 3rd-party lib later); never the umbrella.
//!
//! Skeleton crate. Implementation lands per the design corpus in `docs/design/`
//! (phase3 subsystem docs + phase2 `TOOLKIT.md`), built kernel-first.
