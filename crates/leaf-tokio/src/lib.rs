//! `leaf-tokio` — Default runtime integration: ExecutionFacility + AmbientStore + timer-wheel + ShutdownTrigger over tokio. Depends on leaf-core only.
//!
//! Skeleton crate. Implementation lands per the design corpus in `docs/design/`
//! (phase3 subsystem docs + phase2 `TOOLKIT.md`), built kernel-first.
