//! `leaf-macros` — Thin proc-macro crate: emits one hand-writable const row per macro via absolute leaf_core paths; logic lives in leaf-codegen.
//!
//! Skeleton crate. Implementation lands per the design corpus in `docs/design/`
//! (phase3 subsystem docs + phase2 `TOOLKIT.md`), built kernel-first.
